.. tabs::

   .. tab:: Java
      :tabid: java

      A custom tab with ``tabid`` "java" and a tab name of "Java".

   .. tab:: Kotlin
      :tabid: kotlin

      A custom tab with ``tabid`` "kotlin" and a tab name of "Kotlin".

   .. tab:: Python
      :tabid: python

      A custom tab with ``tabid`` "python" and a tab name of "Python".

Because both tables use the same ``tabid`` values, switching between
tabs on either table also switches between tabs on the other table.

.. tabs::

   .. tab:: Java
      :tabid: java

      A second custom tab with ``tabid`` "java" and a tab name of "Java".

   .. tab:: Kotlin
      :tabid: kotlin

      A second custom tab with ``tabid`` "kotlin" and a tab name of "Kotlin".

   .. tab:: Python
      :tabid: python

      A second custom tab with ``tabid`` "python" and a tab name of "Python".